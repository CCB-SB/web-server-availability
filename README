# Source code for the publication "**On the** **lifetime** **of bioinformatics web** **services**"

The *data* folder contains the files required to perform the data collection and creation of the accessibility matrix.

The *src* folder contains two R scripts:

- download_tools.R was run every day to collect the web-server HTML files
- parse_downloads.R was applied on the results folder to create the accessibility matrix

